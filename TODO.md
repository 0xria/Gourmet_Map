py- [x] Center the auth modal (card) perfectly in the middle of the screen by updating CSS positioning
- [x] Improve auth functions (login, register, signout) by adding better error handling and logging in JavaScript to debug why data is not being received
- [x] Ensure backend auth endpoints are correctly handling requests (check for missing environment variables like SECRET_KEY)
- [x] Test the auth flow after changes (skipped by user)
